name: Run VKE Script

on:
  workflow_dispatch:
    inputs:
      isim_soyisim:
        description: "İsim ve Soyisim"
        required: true
        default: "Ahmet Kaya"
      boy:
        description: "Boy (santimetre cinsinden, ör: 175)"
        required: true
        default: 175
      kilo:
        description: "Kilo (kg)"
        required: true
        default: 70
  push:
    branches:
      - sonuclar

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Debug input_data.txt
      run: |
        # input_data.txt dosyasının içeriğini yazdır
        echo "input_data.txt dosyasının içeriği (satır numarası ile):"
        cat -n input_data.txt  # Satır numaralarıyla içeriği yazdır
        echo "Dosya boyutu:"
        wc -c input_data.txt  # Dosyanın boyutunu kontrol et
        echo "Boşlukları ve özel karakterleri göster: "
        cat -v input_data.txt  # Gizli karakterleri göster

    - name: Check for Invalid Data in input_data.txt
      run: |
        # input_data.txt dosyasındaki boy ve kilo değerlerini al, boşlukları temizle
        boy_cm=$(sed -n '2p' input_data.txt | tr -d '[:space:]')  # Boyu al ve boşlukları temizle
        kilo=$(sed -n '3p' input_data.txt | tr -d '[:space:]')    # Kilo değerini al ve boşlukları temizle

        # Debug: Boy ve Kilo değerlerini yazdır
        echo "Debug: Boy değeri: '$boy_cm', Kilo değeri: '$kilo'"

        # Boy ve Kilo boş mu?
        if [[ -z "$boy_cm" ]] || [[ -z "$kilo" ]]; then
          echo "Hata: Boy veya Kilo boş değer içeriyor!"
          exit 1
        fi

        echo "Boy: '$boy_cm', Kilo: '$kilo'"  # Debug: Değerleri kontrol et

        # Negatif değer kontrolü (Sayısal karşılaştırma ile)
        if [ "$boy_cm" -lt 0 ]; then
          echo "Hata: Boy negatif bir değer!"
          exit 1
        fi

        if [ "$kilo" -lt 0 ]; then
          echo "Hata: Kilo negatif bir değer!"
          exit 1
        fi

        echo "Değerler geçerli. İşleme devam ediliyor."

    - name: Run VKE Script
      run: |
        python main.py
